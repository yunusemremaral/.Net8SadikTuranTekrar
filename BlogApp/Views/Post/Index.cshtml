@model List<Post>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@foreach (var item in Model)
{
    <tr>
        <td>@item.Title</td>
        <td>@item.Description</td>
        <td>@item.Content</td>
    </tr>
}